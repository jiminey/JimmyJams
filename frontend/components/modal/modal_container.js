import Modal from './modal';
import { connect } from 'react-redux'

const mapStateToProps = (state) => ({
    
})

const mapDispatchToProps = {
    return ({

    })
    
}


import default connect(mapStateToProps, mapDispatchToProps)(Modal)